Working commands:

```shell -
python fit_gp.py --model=deep_kernel_gp --gp_mean=zero --gp_covar=rbf --gp_likelihood=gaussian

python fit.py --model=mlp --activation=gelu

python fit.py --model=transformer --n_embd=128
```